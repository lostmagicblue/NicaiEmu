name: Build nicaiemu core (CBE)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y p7zip-full build-essential pkgconf python3 git zip curl patch cmake

      - name: Install Emscripten 3.1.74
        run: |
          git clone https://github.com/emscripten-core/emsdk.git
          cd emsdk
          ./emsdk install 3.1.74
          ./emsdk activate 3.1.74

      - name: Install Rust + emscripten target
        run: |
          curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          rustup target add wasm32-unknown-emscripten

      - name: Fetch sources
        run: |
          git clone --depth 1 --recurse-submodules -b v1.22.2 https://github.com/EmulatorJS/RetroArch.git RetroArch
          git clone --depth 1 https://github.com/AloysHF/NicaiEmu.git nicaiemu

      - name: Patch libretro export names (rust -> emscripten 需要下划线前缀)
        run: |
          cd "$GITHUB_WORKSPACE/nicaiemu"
          python3 - <<'PY'
          import re, glob
          n = 0
          for f in glob.glob('crates/nicaiemu-libretro/src/**/*.rs', recursive=True):
              s = open(f, encoding='utf-8').read()
              t = re.sub(r'(#\[no_mangle\]\s*\n)(pub extern "C" fn )(retro_\w+)(\()',
                         lambda m: m.group(1) + '#[export_name = "_' + m.group(3) + '"]\n' + m.group(2) + m.group(3) + m.group(4),
                         s)
              if t != s:
                  open(f, 'w', encoding='utf-8').write(t)
                  n += len(re.findall(r'#\[export_name', t))
          print('export_name added:', n)
          PY

      - name: Build Rust core and unpack objects
        run: |
          source "$GITHUB_WORKSPACE/emsdk/emsdk_env.sh"
          export PATH="$HOME/.cargo/bin:$PATH"
          cd "$GITHUB_WORKSPACE/nicaiemu"
          sed -i 's/crate-type = \["cdylib"\]/crate-type = ["staticlib"]/' crates/nicaiemu-libretro/Cargo.toml
          cargo build --release --target wasm32-unknown-emscripten -p nicaiemu-libretro
          A="$GITHUB_WORKSPACE/nicaiemu/target/wasm32-unknown-emscripten/release/libnicaiemu.a"
          ls -l "$A"
          mkdir -p /tmp/rustobjs && cd /tmp/rustobjs
          i=0
          for m in $(ar t "$A"); do
            i=$((i+1))
            ar p "$A" "$m" > "$(printf '%04d' $i)_$(basename "$m")"
          done
          echo "object count: $(ls -1 | wc -l)"
          ls -l | head -4
          file "$(ls | head -1)"
          ar rcs "$GITHUB_WORKSPACE/RetroArch/libretro_emscripten.a"

      - name: Link normal + legacy variants
        run: |
          source "$GITHUB_WORKSPACE/emsdk/emsdk_env.sh"
          OBJS=$(ls /tmp/rustobjs/* | tr '\n' ' ')
          cd "$GITHUB_WORKSPACE/RetroArch/emulatorjs"
          mkdir -p out-normal out-legacy
          emmake make -C ../ -f Makefile.emulatorjs \
            TARGET=nicaiemu_libretro.js \
            HAVE_7ZIP=1 HAVE_CHD=1 HAVE_THREADS=0 PTHREAD_POOL_SIZE=0 ASYNC=0 \
            HAVE_OPENGLES3=1 STACK_SIZE=4194304 INITIAL_HEAP=134217728 \
            HAVE_AL=0 HAVE_RWEBAUDIO=1 LIGHT_DEBUG=0 \
            additional_libs="$OBJS" -j$(nproc)
          ls -l ../nicaiemu_libretro.wasm
          cp ../nicaiemu_libretro.wasm ../nicaiemu_libretro.js out-normal/
          emmake make -C ../ -f Makefile.emulatorjs clean
          emmake make -C ../ -f Makefile.emulatorjs \
            TARGET=nicaiemu_libretro.js \
            HAVE_7ZIP=1 HAVE_CHD=1 HAVE_THREADS=0 PTHREAD_POOL_SIZE=0 ASYNC=0 \
            HAVE_OPENGLES3=0 STACK_SIZE=4194304 INITIAL_HEAP=134217728 \
            HAVE_AL=0 HAVE_RWEBAUDIO=1 LIGHT_DEBUG=0 \
            additional_libs="$OBJS" -j$(nproc)
          ls -l ../nicaiemu_libretro.wasm
          cp ../nicaiemu_libretro.wasm ../nicaiemu_libretro.js out-legacy/

      - name: Verify symbols and pack 7z cores
        run: |
          cd "$GITHUB_WORKSPACE/RetroArch/emulatorjs"
          {
            echo "normal wasm bytes : $(stat -c %s out-normal/nicaiemu_libretro.wasm)"
            echo "legacy wasm bytes : $(stat -c %s out-legacy/nicaiemu_libretro.wasm)"
            echo "normal _retro_run : $(grep -ac '_retro_run' out-normal/nicaiemu_libretro.wasm || true)"
            echo "normal _retro_init : $(grep -ac '_retro_init' out-normal/nicaiemu_libretro.wasm || true)"
            echo "normal _retro_load_game : $(grep -ac '_retro_load_game' out-normal/nicaiemu_libretro.wasm || true)"
          } | tee build-info.txt
          mkdir -p out
          printf '%s' '{"name":"nicaiemu","extensions":["cbe"],"options":{},"save":"srm","license":"LICENSE","repo":"https://github.com/AloysHF/NicaiEmu"}' > core.json
          printf '%s' '{"minimumEJSVersion":"4.2.2","version":"0.1.0"}' > build.json
          printf '%s' '{"core":"nicaiemu","buildStart":"ci","buildEnd":"ci","options":{}}' > out/nicaiemu-report.json
          ( cd out-normal && 7z a -t7z ../out/nicaiemu-wasm.data ./* ../core.json ../build.json )
          ( cd out-legacy && 7z a -t7z ../out/nicaiemu-legacy-wasm.data ./* ../core.json ../build.json )
          cp build-info.txt out/
          ls -l out/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nicaiemu-core
          path: |
            RetroArch/emulatorjs/out/nicaiemu-wasm.data
            RetroArch/emulatorjs/out/nicaiemu-legacy-wasm.data
            RetroArch/emulatorjs/out/nicaiemu-report.json
            RetroArch/emulatorjs/out/build-info.txt
